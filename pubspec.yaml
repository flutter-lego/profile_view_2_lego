name: profile_view_2_lego
description: "beautiful profile view 2 lego"
#publish_to: 'none'
repository: https://github.com/flutter-lego/profile_view_2_lego

version: 1.0.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  june_flow_util: #@add
  styled_widget: #@add
  modal_bottom_sheet: #@add
  gap: #@add
  flutter_svg: #@add

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  assets:
    - assets/lego/profile_view_2_lego/
  uses-material-design: true

topics:
  - lego